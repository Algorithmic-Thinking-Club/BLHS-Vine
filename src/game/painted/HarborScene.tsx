// THE GRAND HARBOR — the route's first true scene (floor-plan-born v2)
import PaintedScene from './PaintedScene'
import { HARBOR2 } from './scenes'

export default function HarborScene() {
  return <PaintedScene spec={HARBOR2} />
}
