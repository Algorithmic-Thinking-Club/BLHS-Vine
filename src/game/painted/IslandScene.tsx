// THE HUB ISLAND — cand-1 walkable (Phase A: empty stage, full traversal)
import PaintedScene from './PaintedScene'
import { ISLAND } from './scenes'

export default function IslandScene() {
  return <PaintedScene spec={ISLAND} />
}
