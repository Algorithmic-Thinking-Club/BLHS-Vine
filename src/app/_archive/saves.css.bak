/* The crew roster (§7.7) — a scroll of the device's local voyages, on the kit's paper. */

.sv-veil {
  position: absolute; inset: 0; z-index: 80;
  background: rgba(5, 12, 15, .55);
  display: flex; align-items: center; justify-content: center;
}
.sv-panel {
  position: relative;
  width: min(460px, 90vw); aspect-ratio: 448 / 470;
  background: url('/art/ui/panel-square.png') center / 100% 100% no-repeat;
  image-rendering: pixelated;
  filter: drop-shadow(0 12px 30px rgba(2, 8, 10, .65));
  animation: sv-in 240ms cubic-bezier(.2, 1.2, .35, 1) both;
}
/* the content sits in the measured paper zone. inset % on an absolute child resolves
   against the PANEL's padding box (not the full-width veil, which is why plain % padding
   swallowed the panel) — the correct way to fit content to a fixed-frame asset. */
.sv-inner {
  position: absolute; inset: 15% 16% 15%;
  display: flex; flex-direction: column; align-items: stretch;
}
@keyframes sv-in { from { transform: translateY(12px) scale(.97); opacity: 0; } to { transform: none; opacity: 1; } }

.sv-title {
  font-family: 'Harbormaster', 'Deckhand', monospace;
  font-size: 22px; color: #3c2d1c; text-align: center; margin-bottom: 10px;
}
.sv-list { flex: 1; width: 100%; align-self: stretch; overflow-y: auto; display: flex; flex-direction: column; gap: 6px; }
.sv-list::-webkit-scrollbar { width: 7px; }
.sv-list::-webkit-scrollbar-thumb { background: #8a744f; border-radius: 4px; }

.sv-row {
  width: 100%; text-align: left;
  display: flex; flex-direction: column; gap: 2px;
  background: linear-gradient(180deg, #e6dcc2, #dbcfaf);
  border: 2px solid #8a744f; border-radius: 5px;
  padding: 8px 12px; cursor: pointer;
  font-family: 'Deckhand', monospace;
  transition: transform 120ms ease, box-shadow 120ms ease;
}
.sv-row:hover { transform: translateY(-1px); box-shadow: 0 2px 0 rgba(90, 70, 40, .3); }
.sv-handle { font-size: 17px; color: #33261a; }
.sv-meta { font-size: 13px; color: #6a563c; }
.sv-date { font-size: 12px; color: #8a7a60; }

.sv-actions {
  display: flex; gap: 6px; flex-wrap: wrap;
  padding: 6px 4px 2px;
}
.sv-renamerow { display: flex; gap: 6px; width: 100%; }
.sv-renameinput {
  flex: 1; font-family: 'Deckhand', monospace; font-size: 15px; color: #33261a;
  background: #fff; border: 2px solid #8a744f; border-radius: 4px; padding: 4px 8px;
}
.sv-btn {
  font-family: 'Deckhand', monospace; font-size: 14px; color: #4a3826;
  background: linear-gradient(180deg, #efe6cf, #e2d7b8);
  border: 2px solid #8a744f; border-radius: 4px; padding: 5px 14px; cursor: pointer;
}
.sv-btn:hover { filter: brightness(1.05); }
.sv-go { background: linear-gradient(180deg, #6bbf9e, #3f8f6e); color: #fff; border-color: #2c6b50; }
.sv-del { background: linear-gradient(180deg, #d98f7a, #b8624a); color: #fff; border-color: #8a3e2c; }
.sv-empty { font-family: 'Deckhand', monospace; color: #8a7a60; text-align: center; padding: 20px; }

.sv-close {
  margin-top: 10px; align-self: center;
  min-width: 40%;
  font-family: 'Harbormaster', 'Deckhand', monospace; font-size: 16px; color: #f0e4c8;
  background: url('/art/ui/plank-button.png') center / 100% 100% no-repeat; image-rendering: pixelated;
  aspect-ratio: 512 / 150; border: none; cursor: pointer;
  text-shadow: 0 2px 0 rgba(30, 16, 6, .75);
}
